---
description: Update the globally installed ForgeKit CLI to the latest published version from npm. Use when you want to upgrade forgekit after a new release.
argument-hint: (no arguments needed)
---

## Task

Install or update the ForgeKit CLI to the latest version from npm.

## Steps

Run:

```bash
npm install -g @iconsulting-dev/forgekit@latest
```

Then confirm the installed version:

```bash
forgekit --version
```

Show the output to the user.
