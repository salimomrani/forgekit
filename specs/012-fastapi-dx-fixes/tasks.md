# Tasks: FastAPI DX Fixes

**Input**: Design documents from `/specs/012-fastapi-dx-fixes/`
**Branch**: `012-fastapi-dx-fixes`

## Format: `[ID] [P?] [Story] Description`

- **[P]**: Can run in parallel (different files, no dependencies)
- **[Story]**: Which user story this task belongs to

---

## Phase 1: Setup (no foundational blocking — all stories are independent)

**Purpose**: No shared infrastructure needed. All 4 user stories map to distinct files — tasks run in parallel after initial branch setup.

---

## Phase 2: User Story 1 — IDE resolves imports without manual setup (P1)

**Goal**: `backend/pyrightconfig.json` and root `pyrightconfig.json` generated by ForgeKit.

**Independent Test**: Generate a FastAPI project → verify both `pyrightconfig.json` files exist with correct `venvPath` values.

### Implementation

- [x] T001 [P] [US1] Create `src/templates/fastapi/pyrightconfig.json.hbs` (static template: pythonVersion 3.12, venvPath ".", venv ".venv", include app+tests, extraPaths ["."])
- [x] T002 [P] [US1] Create `src/templates/root/pyrightconfig.json.hbs` (static template: venvPath "backend", venv ".venv", include backend/app+tests, extraPaths ["backend"])
- [x] T003 [US1] Update `src/generators/fastapi/index.ts` — add `renderAndWrite("fastapi/pyrightconfig.json.hbs", path.join(backendDir, "pyrightconfig.json"), data)` to the `Promise.all([...])`
- [x] T004 [US1] Update `src/generators/root/index.ts` — add conditional `renderAndWrite("root/pyrightconfig.json.hbs", ...)` inside `Promise.all([...])` only when `this.config.backendType === "fastapi"`

### Tests

- [x] T005 [US1] Create `src/generators/fastapi/__tests__/fastapi.test.ts` — unit test asserting `backend/pyrightconfig.json` exists and contains `"pythonVersion"` and `"venvPath"`
- [x] T005b [US1] Add negative test to `src/generators/fastapi/__tests__/fastapi.test.ts` — assert root `pyrightconfig.json` is NOT generated when `backendType` is `spring-boot` (call `generateRoot` with non-fastapi config, verify file absent)
- [x] T006 [US1] Update `src/__tests__/e2e.test.ts` S2 — add `expect(fs.pathExists("backend/pyrightconfig.json")).toBe(true)` and `expect(fs.pathExists("pyrightconfig.json")).toBe(true)` (first of 3 sequential S2 edits: T006→T011→T013)

**Checkpoint**: `npm run test:unit` passes, `backend/pyrightconfig.json` and root `pyrightconfig.json` are generated.

---

## Phase 3: User Story 2 — Tests run without deprecation warnings (P1)

**Goal**: `backend/pytest.ini` with asyncio config + `pytest-asyncio` in requirements.

**Independent Test**: Generate a FastAPI project → verify `backend/pytest.ini` contains `asyncio_mode = auto` and `asyncio_default_fixture_loop_scope = function`. Verify `requirements.txt` contains `pytest-asyncio`.

### Implementation

- [x] T007 [P] [US2] Create `src/templates/fastapi/pytest.ini.hbs` (static: `[pytest]`, `asyncio_mode = auto`, `asyncio_default_fixture_loop_scope = function`)
- [x] T008 [P] [US2] Update `src/templates/fastapi/requirements.txt.hbs` — add `pytest-asyncio==0.24.0`
- [x] T009 [US2] Update `src/generators/fastapi/index.ts` — add `renderAndWrite("fastapi/pytest.ini.hbs", path.join(backendDir, "pytest.ini"), data)` to the `Promise.all([...])`

### Tests

- [x] T010 [US2] Update `src/generators/fastapi/__tests__/fastapi.test.ts` (created in T005) — add assertions: `backend/pytest.ini` exists + contains `asyncio_mode`, `requirements.txt` contains `pytest-asyncio`
- [x] T011 [US2] Update `src/__tests__/e2e.test.ts` S2 — add `expect(fs.pathExists("backend/pytest.ini")).toBe(true)` (depends on T006, second of 3 sequential S2 edits)

**Checkpoint**: `npm run test:unit` passes, `backend/pytest.ini` exists with correct content.

---

## Phase 4: User Story 3 — Venv setup documented in quickstart (P2)

**Goal**: `README.md` quickstart includes `python3 -m venv .venv && source .venv/bin/activate` before `pip install`.

**Independent Test**: Generate a FastAPI project → verify `README.md` contains `python3 -m venv .venv`.

### Implementation

- [x] T012 [US3] Update `src/templates/root/README.md.hbs` — in the FastAPI quickstart block, replace `pip install -r requirements.txt` with `python3 -m venv .venv && source .venv/bin/activate && pip install -r requirements.txt`

### Tests

- [x] T013 [US3] Update `src/__tests__/e2e.test.ts` S2 — add assertion that `README.md` contains `python3 -m venv .venv` (depends on T011, third of 3 sequential S2 edits)

**Checkpoint**: Generated `README.md` includes venv creation step.

---

## Phase 5: User Story 4 — HTTP status codes documented in AI rules (P3)

**Goal**: `.claude/rules/backend.md` for FastAPI projects contains explicit HTTP status code convention and venv test command.

**Independent Test**: Generate a FastAPI project → verify `.claude/rules/backend.md` contains `201` and `.venv/bin/pytest`.

### Implementation

- [x] T014 [US4] Update `src/templates/claude-code/rules/backend.md.hbs` — add to FastAPI section: `- **HTTP status codes:** POST (create) → \`201\`, GET → \`200\`, PUT/PATCH → \`200\`, DELETE → \`204\`` and `- **Tests:** run via \`.venv/bin/pytest\` (not bare \`pytest\` — not in PATH until venv is activated)`

### Tests

- [x] T015 [US4] Update `src/generators/claude-code/__tests__/claude-code.test.ts` — add assertion that generated `.claude/rules/backend.md` contains `201` and `.venv/bin/pytest` (ClaudeCodeGenerator owns this file, not FastAPIGenerator)

**Checkpoint**: Generated `backend.md` rules contain HTTP status code table.

---

## Phase 6: Polish & Verification

- [x] T016 [P] Run `npm run typecheck` — confirm zero TypeScript errors
- [x] T017 [P] Run `npm run test:unit` — confirm all unit tests pass including new fastapi.test.ts
- [x] T018 Run `npm run test:e2e` — confirm S2 assertions pass for all 3 new files

---

## Dependencies & Execution Order

- T001, T002, T007, T008 — all parallel (different files, no deps)
- T003 depends on T001 | T004 depends on T002
- T009 depends on T007
- T005 can start after T003 (tests the output of T003+T004)
- T010, T011, T013, T015 extend existing test files — sequential per file
- T012, T014 — independent, parallel
- T016–T018 depend on all implementation tasks

### Parallel Opportunities

```bash
# Launch together (different files):
T001 — fastapi/pyrightconfig.json.hbs
T002 — root/pyrightconfig.json.hbs
T007 — fastapi/pytest.ini.hbs
T008 — requirements.txt.hbs
T012 — README.md.hbs
T014 — backend.md.hbs
```

---

## Implementation Strategy

All 4 user stories are fully independent (different files). Optimal order: T001+T002+T007+T008+T012+T014 in parallel (pure template work), then T003+T004+T009 (generator wiring), then T005+T006+T010+T011+T013+T015 (tests).
